<p class="style">
<%=image_tag("allorders.gif", height: "150px", width: "300px", :alt=>"Current Orders button")%>
</p>
<%@user.orders.each do |order|%>
<table class="blueTable information font">
<tbody>

<tr>
<td>Customer: <%=order.customer_id%></td><td><%=order.created_at.to_date%></td><td> <%=link_to "Order Number #{order.id}", order_path(order)%></td><td>Paid Status: <%=order.paid%></td></tr>

<%end%>
</tbody>
</tr>
</table>
