<p class="style">
<%=image_tag("currentorders.gif", height: "150px", width: "300px", :alt=>"Current Orders button")%>
</p>
<%@customer.orders.drop(1).each do |order|%>
    <%if order.user.id == session[:id]%>
    <table class="blueTable font">
        <tbody>

        <tr>
        <td><%=order.created_at.to_date%></td><td> <%=link_to "Order Number #{order.id}", order_path(order)%></td></tr>
    <%end%>
<%end%>
</tbody>
</tr>
</table>
